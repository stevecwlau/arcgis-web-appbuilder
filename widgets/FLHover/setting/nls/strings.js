define({
  root: ({
    layerSelectorTitle: "Choose which faeture layer will show it's popup when hovered over.",
    layerSelectorNote: "* Layers that are not type feature layer will be disabled in the list.",
    instructions: "Choose a layer to show it's popup when hovered over"
  })
});
